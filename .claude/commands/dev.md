---
description: Run the CLI in dev mode with arguments
allowed-tools: Bash(bun:*)
argument-hint: [cli-args]
---

Run the OpenMind CLI in development mode:

```
bun run src/cli/index.ts $ARGUMENTS
```

Show the output clearly. If there are errors, analyze them and suggest fixes.
